@model Vidly.Models.Movie
@{
    ViewBag.Title = Model.Name;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model.Name</h2>
@*<ul class="list-group">
    <li class="list-group-item">Genre: @Model.Genre.Name</li>
    <li class="list-group-item">Release Date: @Model.ReleaseDate.ToShortDateString()</li>
    <li class="list-group-item">Date Added: @Model.DateAdded.ToShortDateString()</li>
    <li class="list-group-item">Number in Stock: @Model.NumberInStock</li>
</ul>*@
<table class="table table-movie table-hover">
    <thead></thead>
    <tbody>
        <tr>
            <td>Genre:</td>
            <td>@Model.Genre.Name</td>
        </tr>
        <tr>
            <td>Release Date:</td>
            <td>@Model.ReleaseDate.ToShortDateString()</td>
        </tr>
        <tr>
            <td>Data Added:</td>
            <td>@Model.DateAdded.ToShortDateString()</td>
        </tr>
        <tr>
            <td>Number Available</td>
            <td>@Model.NumberAvailable</td>
        </tr>
        <tr>
            <td>Number in Stock:</td>
            <td>@Model.NumberInStock</td>
        </tr>
    </tbody>
</table>


@Html.ActionLink("Edit Movie", "Edit", "Movies", new { id = Model.Id }, new { @class = "btn btn-primary" })

